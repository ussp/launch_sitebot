[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r launch_dam/requirements.txt"]

[start]
cmd = "cd launch_dam && uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
